python setup.py check
- no output is good

python setup.py sdist
- creates tar.gz in dist

pip install twine

# Deploy to test pypi
- twine upload --repository-url https://test.pypi.org/legacy/ dist/sqlpt-0.1.0.tar.gz

# Install from test pypi
pip install -i https://test.pypi.org/simple/ sqlpt

# Deploy to real pypi
- twine upload dist/sqlpt-0.1.0.tar.gz

# Install from pypi (me assuming)
pip install sqlpt


-------------------------------------------
Run tests:

(sqlpt) caine@circuit sqlpt % pwd                                
/Users/caine/workspace/sqlpt
(sqlpt) caine@circuit sqlpt % python3 -m unittest tests.test_unit
